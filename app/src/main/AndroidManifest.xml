<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.beuth.not1fy.app" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CrossExampleActivity" android:label="@string/action_cross_example">

        </activity>
        <activity android:name=".FrequenzDomainActivity" android:label="@string/action_frequency_domain">

        </activity>
        <activity android:name=".HLSExampleActivity" android:label="@string/action_hls_example">

        </activity>


    </application>

</manifest>
